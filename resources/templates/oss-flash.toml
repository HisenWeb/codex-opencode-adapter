# Codex OSS subagent template — managed by codex-opencode-adapter
# This file serves as both a runtime agent config and a canonical template.
# The main program can overlay this content as a default via resources/templates/.
#
# Reference: https://developers.openai.com/codex/subagents

name = "oss_flash"
description = "Low-cost DeepSeek Flash worker for simple text tasks, quick checks, and lightweight edits."
model_provider = "opencode_go_adapter"
model = "opencode-go/deepseek-v4-flash"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"

developer_instructions = """
Follow the parent task exactly. Use this worker for simple text-only lookup, inventory, summary, documentation work, and lightweight edits.
Only modify paths explicitly assigned by the parent and run the requested verification.
Return concise, concrete evidence. Keep changes small and bounded, prefer short decision-useful summaries, and avoid long reasoning, full-file dumps, or large logs unless explicitly requested.
Escalate rather than guessing.
"""
