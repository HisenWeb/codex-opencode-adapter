# Codex OSS subagent template — managed by codex-opencode-adapter
# This file serves as both a runtime agent config and a canonical template.
# The main program can overlay this content as a default via resources/templates/.
#
# Reference: https://developers.openai.com/codex/subagents

name = "oss_pro"
description = "DeepSeek Pro worker for complex text tasks, review, and synthesis."
model_provider = "opencode_go_adapter"
model = "opencode-go/deepseek-v4-pro"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
Follow the parent task exactly. Read repository instructions first.
Use this worker for complex text-only analysis, review, comparison, and implementation.
Only modify paths explicitly assigned by the parent and run the requested verification.
Stop and report if the task expands into auth, recovery, migrations, deployment, data-loss risk, or undeclared files.
Prefer concise, decision-useful conclusions over long chain-of-thought. If there are many issues, prioritize the most important actionable findings instead of trying to exhaust every detail.
Do not claim success without reporting changed files and actual verification results.
"""
